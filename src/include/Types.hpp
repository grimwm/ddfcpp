#ifndef TYPES_HPP__
#define TYPES_HPP__

#include <list>
#include <string>

namespace SymbolX
{
  typedef std::list<unsigned> UnsignedList;
  typedef std::list<char*> PCharList;
  typedef std::list<std::string> StringList;
}

#endif
